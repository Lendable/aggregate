parameters: 
  paths:
    - ./src

  layers:
    - name: Aggregate
      collectors:
        - type: className
          regex: '^Lendable\\Aggregate\\[a-zA-Z_\x7f-\xff][a-zA-Z0-9_\x7f-\xff]*$'

  ruleset: {}
